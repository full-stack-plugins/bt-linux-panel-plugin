---
description: 宝塔面板综合运维入口。诊断/审计/操作全部从这里开始。
argument-hint: "[综合诊断/网站/数据库/服务/防火墙/SSL/计划任务/通知]"
skills: bt-panel-ops
---

Use the `bt-panel-ops` skill for this request:

$ARGUMENTS

If the request concerns MCP setup, read `bt-mcp-setup` first.
If the request concerns a specific tool selection, read `bt-mcp-tools`.